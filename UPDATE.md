# SeaScribe 更新日志

## v4.3.2

### 启动界面优化
- 初始延迟从 500ms 降至 100ms，日志立即开始输出，不再白屏卡顿
- 条间间隔从 60ms 降至 20ms，全部检查更快完成
- 新增 9 阶段分组：运行环境 → 配置系统 → 样式系统 → 插件系统 → 核心 DOM → 模态弹窗 → 外部依赖 → 会话状态 → 汇总
- 细化条目至 31 项：新增浏览器检测、在线状态、CSS 文件计数、Logo 加载验证等
- 阶段间加入分隔线标题，视觉更清晰

## v4.3.1

### 代码质量重构
- 统一版本号至 v4.3.0（index.html、splash.js 此前残留 v4.2.0）
- 移除 `picker/index.js` 和 `picker/animation.js` 中重复的 `esc()` 函数，统一使用 `SeaScribe.esc()`
- 提取 `SeaScribe.delay()` 到 `core.js`，消除 `random.js` 和 `animation.js` 中的重复定义
- 修复 `picker/animation.js` 中 rAF 竞争条件：增加代数计数器防止旧帧错误 resolve 新 Promise
- 简化 `picker/index.js` 中冗余的 `CustomSelect` 初始化判断
- 删除 `index.html` 中空的 `<!-- Student lists -->` 注释块

### Bug 修复
- 修复化学/英语插件文件选择下拉框扫描后 CustomSelect 选项不刷新、默认文件不显示
- 修复 CustomSelect 下拉框滚动条样式缺失（与主站 `.range-select` 统一滚动条风格）
- 修复 `csStaggerIn` 动画 opacity:0 导致下拉选项长时间空白、translateY 导致抖动

## v4.3.0

### 界面全面适配移动端
- 主站和管理后台全部页面适配手机/平板，两档断点（≤768px / ≤480px）
- 顶栏在窄屏不换行，逐步隐藏文字仅留图标、点名结果、版本号
- 卡片网格手机端单列，学科卡片自适应缩小
- 管理后台所有表格支持横向滚动，表单行纵向堆叠
- 模态弹窗在手机端 96vw 全宽
- 登录/注册/绑定页面完全适配

### 过渡动画
- 管理后台补全动画：页面卡片淡入、按钮点击缩放反馈、表格行平滑过渡、表单聚焦光环
- 登录卡片入场动画、上传区交互动画
- 主站登录状态指示器滑入动画
- 所有动画使用 opacity + transform（GPU 加速）

### 自定义下拉组件
- 全新 `custom-select.js` 替换全部原生 select 元素
- 支持键盘导航（↑↓ Enter Esc）、点击外部关闭、交错滑入动画
- 箭头旋转 180° 展开/收起过渡
- 深色模式适配
- 管理后台和主站全部统一，化学/英语插件同时适配

### 用户管理增强
- 表格从 9 列精简为 7 列（移除用户ID、签名列），文字不再换行错位
- 点击表头排序（用户名/姓名/昵称/班级/角色），▲▼ 指示方向
- 筛选栏默认展开，点击搜索框收起；容器级 focusout 防止误收起
- 编辑表单按「账号信息」「个人信息」「安全」分组
- 编辑时显示用户 ID（只读）
- 姓名池新增绑定状态列（已绑定用户名 / 未绑定）
- 点名记录新增逐条删除功能

### 登录系统优化
- 登录失败改用 toast 弹窗提示（401 改 400 防止页面刷新）
- toast 统一为普通提示（绿左边框）和警告（红左边框+⚠️）
- 密码框新增一键清除按钮、显示/隐藏改为 SVG 睁眼/闭眼图标
- 注册昵称提示修正为「给自己起个好记的名字」

### 点名系统
- 头像和上次点名时间位置互换（时间在上、头像在下）
- 无头像用户显示默认剪影 SVG
- 最短滚动动画降至 2 秒

### 导航整理
- 管理后台页面重新排序（我的资料→用户管理→姓名池→点名记录→英语文件→配置管理）
- 姓名池图标改为 🏫，所有导航图标唯一化
- 主站管理后台入口移至顶栏独立按钮，显示登录状态
- 退出登录按钮换为手绘 SVG 图标

### 其他修复
- `scanURLs` 属性名统一（修复化学/英语插件服务器扫描失败）
- `history.replaceState` 消除浏览器 skippable history 警告
- 404/401 状态码修正
- 深色模式下拉箭头亮度适配
- Splash 启动日志从 19 项扩展到 29 项
- 全部下拉框统一美化（自定义三角形箭头）

---
## v4.2.0

### 代码优化
- 统一 HTML 转义函数：`SeaScribe.esc()` 消除全局所有局部定义
- 修复 `records.js` 重复加载
- 简化 `DictationEngine` 类为 `SeaScribe.shuffleAndPick()` 普通函数
- 抽取统一 Markdown 渲染器 `main/js/markdown.js`，`about.js` 和 `changelog.js` 共用
- 管理后台复用主站主题 CSS 变量，消除重复定义
- `picker.css` 从 `main/js/picker/` 移至 `main/css/`
- 主题切换提取为 `Admin.toggleTheme()` + `SeaScribe.applyTheme()`，主站与后台统一 localStorage key
- CSS `transition` 属性统一使用 `background-color`
- 新建 `plugin-utils.js`：提取 `loadConfig`/`scanDir`/`refreshCount`/`parseCSV` 公共方法，english/chemistry 插件各减少约 50 行
- `core.js` 拆分出 `engine.js`（`shuffleAndPick`），core.js 仅保留 `SubjectRegistry` + `esc`
- 删除点名系统 `parseCSV()` 死代码、`.topbar-author` 死 CSS

### Bug 修复
- 修复 `server.py` logout 接口引用未定义变量导致 500 错误、`list_directory` 死代码、register 接口响应顺序错误
- 修复 `cards.js` `configUI` 传入多余参数、`timestamp.js` JSDoc 过时
- 修复退出登录后登录表单无法输入
- 修复注册页 `[redacted]` 残留文本导致 `ReferenceError`
- 修复主站与管理后台主题切换不同步

### 界面增强
- 学生资料页新增班级显示、用户管理页新增用户ID/班级列和筛选、姓名字段改为姓名池下拉选择
- 登录页和绑定页新增顶栏（logo + 主题切换 + 退出），绑定页支持退出登录
- 深色模式禁用输入框不再白底突兀、主题切换添加平滑过渡动画

### 注册系统
- 新增学生自助注册：用户名（字母开头3-20位）+ 昵称 + 密码
- 注册表单实时校验（边框变色 + 提示文字）、Enter 键提交、注册中禁用输入框
- 注册后自动进入绑定姓名流程
- 登录/注册卡片支持 hash 路由（`#/login`、`#/register`），浏览器前进后退可用

### 文档更新
- 移除 `docs/` 目录，文档移至根目录
- RULES.md 新增模块架构章节、更新名单规范（CSV → JSON roster）、文件计数
- PLUGINS.md 更新 `loadConfig` 模板为 `PluginUtils.loadConfig`
- README 修正离线限制说明

---
## 近期更新

### 界面优化
- 管理后台改为主站顶栏导航风格，去掉侧栏
- 配色统一为主站主题（红 accent、主站色板）
- 点名弹窗查看/清空时间戳、头像上传

### 用户系统增强
- 新增「姓名」字段（displayName），绑定点名名单，管理员设置后不可自改
- 昵称独立，自由修改
- 头像支持本地上传，不暴露文件路径
- 资料保存与密码修改分离

### Bug 修复
- 修复配置管理页表单控件缺失 name 属性
- 修复 401 无限重载、头像上传 body 丢失
- 修复旧密码空字符串误触发校验
- 修复 start.bat 命令行乱码

---
## v4.0.1

### 安全加固
- 密码升级为 PBKDF2-HMAC-SHA256（600,000 迭代），替代旧 SHA256
- session 增加 4 小时过期时间，自动清理过期会话
- 新增登录限流：同 IP 5 次失败锁定 60 秒
- session token 通过 HttpOnly Secure SameSite=Strict Cookie 下发，XSS 不可窃取
- 修复路径穿越漏洞（picker-timestamps / delete-file 文件名注入）
- 请求体大小限制（普通 API 1MB，上传 60MB）
- user-avatar 接口补登录校验

### 用户系统
- 每个用户新增唯一 UUID（`uid`），API 响应中返回
- users.json 结构改为 `{by_id: {uid: {...}}, by_name: {username: uid}}`
- 昵称、头像字段增加长度限制（100 / 2000 字符）

---
## v4.0.0

### 管理后台系统
- 新增 `admin/` 独立管理后台 SPA，访问 `/admin/` 进入
- 基于 token 的鉴权系统（sha256 加盐密码），session 管理
- 三种角色：管理员（全局管理）、教师（配置+文件）、学生（个人资料）

### 用户管理
- 管理员可 CRUD 用户：用户名（英文）、昵称（中文/日文）、头像（emoji 或图片 URL）、角色
- 所有用户可自行修改个人资料、密码
- 用户数据存储在 `admin/_store/`（不入库），HTTP 直接访问被拦截

### 可视化配置管理
- 主配置 / 化学 / 英语 / 点名四个配置文件可通过表单编辑
- 支持文本、数字、布尔、数组字段，保存后刷新页面生效

### 英语文件上传
- 管理员/教师可通过管理后台上传 xlsx/csv 文件到服务器
- 支持拖拽/点击上传，文件列表展示，可删除

### 点名系统增强
- 点名结果展示时自动加载用户头像（有则显示在名字上方）
- 查看/清空时间戳功能保留在点名弹窗中
- 顶栏新增「🔧 管理后台」入口

---
## v3.5.0

### 弹窗系统统一
- 所有弹窗统一右上角 ✕ 关闭，移除底部关闭按钮
- 弹窗打开/关闭带淡入淡出和缩放动画
- 关闭逻辑抽取为公共模块 `modal.js`，支持 ✕ + 点击遮罩关闭

### 顶栏精简
- 更新日志、系统日志、关于合并为「⋯ 更多」下拉菜单
- 随机点名按钮改为实心主题色，hover 反色

### 界面细节
- 插件图标重绘为手绘 SVG（化学试管、英语字母A圆形徽章）
- 关于按钮图标换为手绘 SVG（圆圈感叹号）
- 开屏界面支持按任意键关闭
- 配置文件精简注释，只保留可选值列表

---
## v3.4.0

### 点名系统完全重做
- 模块化架构：`random.js`（算法）、`timestamp.js`（时间戳）、`animation.js`（编排）、`display.js`（展示）、`index.js`（入口）
- 三种随机方式：
  - **纯随机** — Fisher-Yates 洗牌，完全随机
  - **二分法** — 每轮洗牌后对半分，随机淘汰一半，逐步缩小直到剩一人
  - **时间加权随机法** — 多轮随机缩小子集至 ≤5 人，优先从未被点过的人中选，否则选距上次最久者
- 双层动画系统：修饰动画（全屏滚动 + 彩带）+ 过程动画（可选，展示算法步骤）
- 过程动画支持全屏 / 窗口化两种模式
- 时间戳记录存于服务端（`data/picker/`），支持开关
- CSV 第 2 列改为"个性签名"，结果展示姓名 + 签名 + 上次点名时间
- 所有配置集中管理（`config/picker/config.js`）
- 弹窗界面美化：分组布局、平滑展开、选中高亮
- 点名按钮改为 🎲，结果在左按钮在右

---
## v3.3.0

### 化学数据文件化
- 化学元素数据迁移至独立 CSV 文件（`data/chemistry/elements.csv`），可用 Excel 或记事本编辑
- 化学页新增文件选择功能：🔍 扫描服务器目录、下拉框切换文件、📥 从本地上传 CSV
- 打开化学页自动加载默认数据，无需手动操作

### 关于页面
- 顶栏新增 ℹ️ 关于按钮，点击弹窗展示项目说明（内容来自 README）

### 界面调整
- 答案卡片标签与弹窗标题由红色改为正文色，视觉更统一
- Logo 切换为 PNG 格式

### 点名动画
- 点名定格时触发彩色纸屑特效（canvas-confetti）
- 优化动画时序，整体节奏更紧凑

---

## v3.2.0

### 名单系统重构
- 名单从 JS 硬编码改为 CSV 文件，放在 `data/stdlist/` 下即可自动识别
- 顶栏下拉框自动扫描 `/api/stdlist-files`，无需修改代码
- CSV 第 2 列支持头衔，点名定格后在全屏显示
- 点名动画优化：滚动只显姓名，定格显示姓名+头衔，缩小后仅姓名
- 支持添加任意数量班级，只需放入新的 CSV 文件
- 旧的 JS 名单文件已移除

### 启动与日志
- 启动日志逐行滚动，12 项详细检查（主题/配置/插件/DOM）
- 每条日志带成功/失败文字说明
- 顶栏新增 📋「系统日志」按钮，可随时查看状态
- 有错误时按钮右上角显示红色错误计数

### 界面优化
- 更新日志弹窗改进：关闭按钮固定在底部，不随内容滚动
- 顶栏版本号移到文字右上方，不可点击
- 📜 更新日志按钮加图标
- 项目根目录整理：文档移入 `docs/`，logo 移入 `main/`

### 工程化
- `server.py` 为文本文件添加 UTF-8 charset 头，解决 CSV 乱码
- 新增 `start.bat` 一键启动脚本
- 新增 `RULES.md` 第 9 条「文件写入规范」（禁用 PowerShell 写文件）
- CSV 解析改为纯 JS，去掉 SheetJS 依赖

---

## v3.1.0

### 品牌与工程化
- 设计项目 logo（波浪弧线+笔尖+墨滴，统一图形）
- logo 应用于顶栏、开屏动画、favicon、README
- 接入 Git 版本管理，托管于 [GitHub](https://github.com/qxdho/SeaScribe)
- 目录拍平：v3.0.0 内容提至根目录，旧版本归档至 `archive/`
- 制定 `RULES.md` 编码规范（8 条规则）
- 遵循语义化版本（SemVer），每次发版打 tag

---

## v3.0.0

### 代码模块化
- CSS 拆分为 9 个文件（theme / base / topbar / pages / controls / cards / splash / changelog / responsive）
- JS 拆分为 9 个文件（core / theme / navigator / controls / cards / picker / changelog / splash / app）
- `index.html` 瘦身为纯 HTML 骨架，仅保留主题初始化 4 行内联脚本
- 统一命名空间 `window.SeaScribe`
- v2.0 原封不动保留

### 界面优化
- Topbar 去作者名，更新日志移至右侧紧挨夜间模式
- Topbar 版本号缩小为小字灰色
- 控制栏按钮回归单行，紧跟字号控件
- 开屏添加「本项目使用 vibecoding 开发」

### 工程化
- 接入 Git 版本管理，托管于 [GitHub](https://github.com/qxdho/SeaScribe)
- 添加 `.gitignore`（排除 data/、zip、reasonix 等敏感文件）
- v1.0 / v2.0 旧版本源码归档至仓库
- 制定 `RULES.md` 编码规范（8 条规则，含 Git 工作流、版本号管理）
- 遵循语义化版本（SemVer），每次发版打 tag

---

## v2.0

### 布局双模式
- 新增列表/网格双模式切换（`☰` / `⊞` 按钮）
- 网格模式：卡片方阵排列，适合投屏浏览
- 列表模式：卡片横向排列，答案跟随题目下方，CSS Multi-Column 原生列优先填充
- 切换模式时伴有过渡动画
- 网格和列表各自独立记忆列数和字号

### 配置系统重构
- 所有默认参数从配置文件读取，零硬编码兜底
- 配置文件统一在 `config/` 目录，按学科分文件夹
- 网格模式、列表模式的列数和字号分别可配置
- 配置文件添加详细中文注释和取值说明

### 英语插件增强
- 默认列表模式，2 列显示
- 按钮「🔍 从服务器获取」进入时自动扫描 `data/english/` 目录
- 支持本地 `.xlsx` / `.csv` 文件导入
- 听写列和答案列可通过步进器调整，切换即时生效

### 化学插件
- 默认网格模式，4 列显示
- 价层电子式和轨道式同时展示
- H~Kr 36 元素，范围选择器

### 界面优化
- 答案文字正常换行（`pre-wrap`），去掉虚线分隔
- 卡片序号跟随字号缩放
- 控制栏紧凑化，增大卡片空间
- 随机点名动画约 2 秒
- 默认浅色主题
- favicon 图标

### 项目结构
- 目录重构：`main/`（代码）、`config/`（配置）、`plugins/`（插件）、`data/`（数据）
- `server.py` 提供通用 `/api/<name>-files` 接口，禁止目录浏览
- URL hash 路由：`#/english`、`#/chemistry`
- 更新日志独立为 `update.md`，点击「更新日志」按钮动态加载

### 开屏动画
- 启动时显示品牌动画
- 逐项检查：配置加载、插件注册、页面元素
- 点击任意位置关闭

---

## v1.0

- 化学插件：价层电子式 + 轨道式，H~Kr 36 元素
- 英语插件：xlsx 本地导入
- 随机点名全屏动画
- 夜间模式、无极字号、自定义列数
- 开屏动画、标签页图标
- 插件化架构
- 学生名单管理
