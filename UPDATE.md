# SeaScribe 更新日志

## v4.4.0

### 全局动画全面升级

**新增缓动体系**
- 新增 `--spring`（弹性）、`--ease-out`（平滑减速）、`--bounce`（弹跳）CSS 缓动变量
- 主站和管理后台统一使用

**开屏动画**
- Logo：新增旋转弹性入场（5 阶段 bounce：0°→rotate(-15°)→rotate(3°)→rotate(-1°)→0°）
- 标题/作者/版本：新增 blur 模糊渐显 + 弹性缓动
- 日志行：逐条渐入动画
- 提示文字：入场后增加持续浮动效果
- 退出动画：缩放 + 模糊消失

**卡片动画**
- 听写卡片：fadeUp 新增 3D rotateX 旋转 + blur 入场
- 卡片 hover：上浮 2px + 阴影
- 答案展开：改用弹性缓动
- 网格切换：gridPulse 增加缩放弹性

**页面过渡**
- pageIn/pageOut：新增 scale + blur
- 学科卡片 hover：加大上浮距离 + 边框高亮 + 阴影增强

**弹窗**
- 遮罩层：新增 backdrop-filter 毛玻璃模糊效果
- 卡片入场：弹性缓动 + blur 渐显
- 关闭按钮 hover：旋转 90°

**点名动画**
- 弹窗入场：弹性缓动 + blur
- pickPop 点名定格：三阶段弹性缩放（1.5→0.95→1）
- procStepIn 过程步骤：新增 blur

**顶栏**
- 更多菜单：从按钮位置弹性缩放展开 + 菜单项 hover 滑入
- 登录状态：blur 渐显

**管理后台**
- 页面卡片：弹性缓动 + blur
- 按钮点击：弹性缩放反馈
- Toast 通知：弹性滑入
- 导航标签 hover：微上浮
- 表格行 hover：微缩放

---

## v4.3.3

### 移动端弹窗适配
- 修复「更新日志」「系统日志」「关于」三个弹窗在手机/平板端未正确适配的问题
- 根因：ID 选择器优先级高于响应式类选择器，导致宽度覆盖失效
- 768px 断点：新增对应 ID 选择器规则，弹窗宽度 92vw
- 480px 断点：弹窗宽度 96vw，表格横向滚动、代码块自动换行、图片自适应

---

## v4.3.2

### 启动界面优化
- 初始延迟从 500ms 降至 100ms，日志立即开始输出，不再白屏卡顿
- 条间间隔从 60ms 降至 20ms，全部检查更快完成
- 9 阶段 31 项分组检查：运行环境→配置→样式→插件→DOM→弹窗→依赖→会话→汇总
- 新增浏览器检测、在线状态、CSS 文件计数、Logo 加载验证

---

## v4.3.1

### 代码质量重构
- 统一版本号至 v4.3.0（此前 index.html、splash.js 残留 v4.2.0）
- 移除重复的 `esc()` 函数，统一使用 `SeaScribe.esc()`
- 提取 `SeaScribe.delay()` 到 core.js，消除 random.js 和 animation.js 重复定义
- 修复 rAF 竞争条件：代数计数器防止旧帧错误 resolve 新 Promise
- 简化冗余的 CustomSelect 初始化判断，删除空注释块

### Bug 修复
- 修复插件文件选择下拉框扫描后 CustomSelect 选项不刷新、默认文件不显示
- 修复 CustomSelect 下拉框滚动条样式缺失
- 修复 csStaggerIn 动画 opacity:0 导致下拉选项长时间空白、translateY 导致抖动

---

## v4.3.0

### 移动端全面适配
- 主站和管理后台全部页面适配手机/平板，两档断点（≤768px / ≤480px）
- 顶栏窄屏逐步隐藏文字仅留图标，卡片网格手机端单列
- 管理后台表格横向滚动，表单行纵向堆叠，登录/注册/绑定页完全适配

### 自定义下拉组件
- 全新 CustomSelect 替换全部原生 select，键盘导航、交错滑入动画、深色模式适配
- 主站和管理后台全部统一

### 用户管理增强
- 表格精简 7 列，点击表头排序（▲▼ 指示方向）
- 筛选栏默认展开，容器级 focusout 防误收起
- 姓名池新增绑定状态列，点名记录新增逐条删除

### 登录系统优化
- 登录失败改用 toast 提示，密码框新增清除按钮、显示/隐藏 SVG 图标
- 学生自助注册：用户名+昵称+密码，实时校验，Enter 提交

### 其他
- 点名头像与时间位置互换，无头像显示默认剪影
- 管理后台页面重排序，导航图标唯一化
- 主站管理入口移至顶栏，显示登录状态

---

## v4.2.0

### 代码优化
- 统一 HTML 转义函数 `SeaScribe.esc()`，消除所有局部定义
- 抽取公共模块：`markdown.js`（渲染）、`plugin-utils.js`（loadConfig/scanDir/refreshCount/parseCSV）
- 拆分 `core.js`→`engine.js`，主题切换主站与后台统一 localStorage key
- 管理后台复用主站 CSS 变量，消除重复定义

### Bug 修复
- 修复 server.py logout 500 错误、register 响应顺序错误
- 修复退出登录后表单无法输入、注册页残留文本 ReferenceError
- 修复主站与管理后台主题切换不同步

### 界面增强
- 学生资料页新增班级显示，用户管理新增班级列和筛选
- 登录页和绑定页新增顶栏（logo+主题+退出）
- 深色模式输入框不再白底突兀

### 注册系统
- 新增学生自助注册，实时表单校验，注册后自动进入绑定姓名流程
- 登录/注册卡片支持 hash 路由（`#/login`、`#/register`）

---

## v4.1.0

### 界面优化
- 管理后台改为顶栏导航风格，去掉侧栏，配色统一为主站主题
- 点名弹窗查看/清空时间戳、头像上传

### 用户系统增强
- 新增「姓名」字段（displayName），绑定点名名单，管理员设置后不可自改
- 昵称独立自由修改，头像支持本地上传
- 资料保存与密码修改分离

### Bug 修复
- 修复配置管理页表单缺失 name 属性、401 无限重载
- 修复头像上传 body 丢失、旧密码空字符串误触发校验
- 修复 start.bat 命令行乱码

---

## v4.0.1

### 安全加固
- 密码升级 PBKDF2-HMAC-SHA256（600,000 迭代），替代旧 SHA256
- session 增加 4 小时过期，自动清理过期会话
- 登录限流：同 IP 5 次失败锁定 60 秒
- session token HttpOnly Secure SameSite=Strict Cookie 下发
- 修复路径穿越漏洞，请求体大小限制
- 每个用户新增唯一 UUID

---

## v4.0.0

### 管理后台
- 新增独立管理后台 SPA，token 鉴权（sha256 加盐密码）
- 三种角色：管理员（全局）、教师（配置+文件）、学生（个人资料）
- 用户 CRUD、可视化配置管理、英语文件上传

### 点名增强
- 点名结果自动加载用户头像
- 查看/清空时间戳，顶栏「🔧 管理后台」入口

---

## v3.5.0

### 弹窗与顶栏
- 所有弹窗统一右上角 ✕ 关闭，淡入淡出+缩放动画
- 关闭逻辑抽取为公共模块 modal.js
- 更新日志/系统日志/关于合并为「⋯ 更多」下拉菜单
- 随机点名按钮实心主题色，hover 反色
- 插件图标重绘为手绘 SVG

---

## v3.4.0

### 点名系统重做
- 模块化架构：random.js + timestamp.js + animation.js + display.js + index.js
- 三种随机方式：纯随机（Fisher-Yates）、二分法（对半淘汰）、时间加权（子集+优先未点）
- 双层动画：修饰动画（全屏滚动+彩带）+ 过程动画（可选）
- 时间戳存服务端，支持开关
- 配置集中管理于 `config/picker/config.js`

---

## v3.3.0

### 化学数据文件化
- 元素数据迁移至独立 CSV（`data/chemistry/elements.csv`）
- 化学页新增文件选择：扫描服务器目录、切换文件、本地上传 CSV

### 其他
- 顶栏新增 ℹ️ 关于按钮，展示 README 内容
- 点名定格触发 confetti 彩带特效
- Logo 切换为 PNG 格式

---

## v3.2.0

### 名单系统重构
- 名单从 JS 硬编码改为 CSV 文件，`data/stdlist/` 下自动识别
- CSV 第 2 列支持头衔/签名
- 启动日志 12 项逐行检查，顶栏 📋「系统日志」按钮，错误计数

### 工程化
- server.py 为文本文件添加 UTF-8 charset
- 新增 start.bat 一键启动
- 新增 RULES.md 编码规范

---

## v3.1.0

### 品牌与工程化
- 设计项目 logo（波浪弧线+笔尖+墨滴）
- 接入 Git 版本管理，托管 GitHub
- 目录拍平，旧版归档 `archive/`
- 制定 RULES.md 编码规范

---

## v3.0.0

### 代码模块化
- CSS 拆分 9 文件、JS 拆分 9 文件，统一命名空间 `window.SeaScribe`
- index.html 瘦身为纯 HTML 骨架
- 接入 Git，添加 .gitignore，旧版归档

---

## v2.0

### 布局双模式
- 网格/列表双模式切换，各自独立记忆列数和字号
- 配置系统重构：`config/` 目录按学科分文件夹，零硬编码兜底
- 英语插件：默认列表模式，支持 xlsx/csv 导入，可调听写/答案列
- 化学插件：默认网格模式，价层电子式+轨道式，H~Kr 36 元素

### 项目结构
- 目录重构：`main/` `config/` `plugins/` `data/`
- 通用 `/api/<name>-files` 接口
- URL hash 路由、开屏动画、favicon

---

## v1.0

- 化学插件：价层电子式 + 轨道式，H~Kr 36 元素
- 英语插件：xlsx 本地导入
- 随机点名全屏动画
- 夜间模式、无极字号、自定义列数
- 开屏动画、标签页图标
- 插件化架构、学生名单管理
