// ================================================================
//  化学学科 — 配置文件
//  修改后刷新页面即可生效
// ================================================================

window.__CHEMISTRY_CONFIG__ = {

  // 【出题数量】每次随机抽几道题，填 1 ~ 36
  defaultCount: 8,

  // 【初始列数】刚打开化学时用几列显示，填 1 ~ 6
  defaultColumns: 4,

  // 【默认布局】刚打开化学时是网格还是列表
  //   "grid" = 网格（卡片方阵排列）
  //   "list" = 列表（卡片横向排列）
  defaultLayout: "grid",

  // 【初始字号】刚打开化学时文字大小，填 60 ~ 200（百分比）
  defaultFontSize: 100,

  // —————— 网格模式的设置 ——————
  // 当你从列表切换到网格时，会自动应用以下设置

  //   网格模式下显示几列，填 1 ~ 6
  gridColumns: 4,

  //   网格模式下的字号，填 60 ~ 200
  gridFontSize: 100,

  // —————— 列表模式的设置 ——————
  // 当你从网格切换到列表时，会自动应用以下设置

  //   列表模式下显示几列，填 1 ~ 6
  listColumns: 2,

  //   列表模式下的字号，填 60 ~ 200
  listFontSize: 100,

  // —————— 元素范围 ——————
  // 化学元素从 H(氢) 开始，到 Kr(氪) 结束，共 36 个
  // 你可以限定只出其中一部分元素

  //   起始元素索引：0 = H(氢)，29 = Zn(锌)
  defaultRangeStart: 0,

  //   结束元素索引（不包含）：30 = 到 Zn(锌)，36 = 到 Kr(氪)
  defaultRangeEnd: 30,

  // —————— 数据文件 ——————
  // 默认 CSV 文件路径（相对于页面），包含 symbol,name,electron,orbital 四列
  // 进入化学页时自动加载，也可在界面上选择其他 CSV 文件
  dataURL: "data/chemistry/elements.csv",

  // —————— 服务器文件扫描 ——————
  // 部署到服务器后，点击「🔍 从服务器获取」自动扫描 data/chemistry/ 目录
  scanURLs: [
    "/api/chemistry-files"
  ]

};
