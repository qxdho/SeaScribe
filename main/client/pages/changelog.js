/* ============================================================
   SeaScribe — Changelog (ES Module)
   ============================================================ */

import { initMarkdownModal } from '../utils/markdown-modal.js';

initMarkdownModal('btn-changelog', 'changelog-overlay', 'btn-changelog-close-x', 'changelog-body', 'UPDATE.md');
